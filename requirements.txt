# ── Core (P0 eval pipeline; pure-python parts only need numpy/pyyaml/pillow) ──
numpy>=1.24
pyyaml>=6.0
pillow>=9.5
pandas>=2.0
tqdm>=4.66

# ── Inference / models (install on the GPU server) ──
# Torch is intentionally NOT pinned here: install the CUDA build that matches the
# server driver first, e.g.
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
torch
torchvision
transformers>=4.49.0          # Qwen2.5-VL requires >=4.49
accelerate>=0.34
qwen-vl-utils>=0.0.8          # Qwen2.5-VL image/video preprocessing helper
einops
sentencepiece
timm                          # InternVL vision tower
av                            # video frames (optional, nuScenes sequences)

# ── Post-training (P1/P2; install when you reach them) ──
# ms-swift is the recommended trainer for Qwen2.5-VL LoRA-SFT / DPO / ORPO.
#   pip install "ms-swift[llm]" -U
# peft / trl are pulled in transitively but listed for clarity.
# ms-swift
# peft>=0.12
# trl>=0.11

# ── Optional demo ──
# gradio>=4.0
# fastapi
# uvicorn
